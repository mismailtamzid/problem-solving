/*
#include<stdio.h>
#include<string.h>
int main(){
 int n,length, i=0, pairs=0;
// scanf("%d", &n);
 scanf("%d", &length);
 char binaryNumber[length];
 scanf("%s", &binaryNumber);
 printf("%s",binaryNumber);
for(i=0; i<=strlen(binaryNumber)-1; i++){
 /*
 if((binaryNumber[i] == 1 && binaryNumber[i+1]== 0) || (binaryNumber[i] == 0 && binaryNumber[i+1]== 1)){
       pairs+=1;
} */
//if(binaryNumber[i]!= 1)
       printf("\n %c  = ", typename(binaryNumber[i]));
    }
return 0;
}
*/
/*
 for(i=0; binaryNumber[i] != "\0"; i++){
    if((binaryNumber[i] == 1 && binaryNumber[i+1]== 0) || (binaryNumber[i] == 0 && binaryNumber[i+1]== 1)){
       pairs+=1;
    }
 }
 */
